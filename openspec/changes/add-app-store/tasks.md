## 1. Инструменты и репозиторий стора

- [ ] 1.1 Установить GitHub CLI (`winget install GitHub.cli`) и выполнить `gh auth login`; проверка: `gh --version` и `gh auth status` показывают готовность
- [ ] 1.2 Создать публичный remote `dziominpavel.github.io` на GitHub и выполнить первый push (только по прямой команде владельца); проверка: репозиторий открывается на GitHub, содержит README. Локально git-репо уже инициализирован
- [ ] 1.3 Дозавести каркас проекта: `registry.yaml` (запись FogMap для пилота), `templates/`, `static/` (css/js), `generate.py`, `.gitignore` (OpenSpec/OpenCode-структура уже создана init); проверка: структура каталогов на месте
- [ ] 1.4 Зарегистрировать бесплатный аккаунт GoatCounter и получить site ID; проверка: site ID получен и записан (понадобится в задаче 3.6)

## 2. Контракт в пилотном проекте (FogMap)

- [ ] 2.1 Создать `store.yaml` в корне FogMap (name, description-черновик, icon, requirements «Android 8+»); проверка: YAML парсится, все обязательные поля заполнены
- [ ] 2.2 Положить обязательную иконку png 512×512 и указать путь в `icon`; проверка: файл существует по указанному пути
- [ ] 2.3 Собрать 3–5 скриншотов единого размера (1080×2400) в `screenshots/`; проверка: 3–5 файлов, одинаковые пропорции
- [ ] 2.4 Проверить файл `version` в FogMap (уже есть, формат MAJOR.MINOR.PATCH); проверка: `python scripts/check-version.py` проходит
- [ ] 2.5 Добавить `dist/` в `.gitignore`, если артефакты ещё не закрыты; проверка: `git status` не показывает содержимое `dist/`

## 3. Release-скрипт (эталон + пилот)

- [ ] 3.1 Написать эталон `release.ps1`/`release.bat`: проверки (файл version есть, dist/ не пуст, icon существует), `git tag v$(version)`, `gh release create` c ассетами из `dist/`, печать версии скрипта; проверка: сухой прогон в FogMap с пустым dist падает с понятной ошибкой и не создаёт тег
- [ ] 3.2 Прогнать реальный пилотный релиз FogMap (текущая версия из файла version); проверка: на GitHub появился релиз с тегом `vX.Y.Z`, ассет `FogMap-X.Y.Z.apk`, предыдущие релизы не тронуты
- [ ] 3.3 Проверить негативные ветки скрипта: нет иконки → отказ; тег уже существует → отказ без перезаписи; `gh` не авторизован → отказ без частичной публикации; проверка: каждая ветка даёт понятную ошибку и ноль изменений на GitHub
- [ ] 3.4 Расширить `build-apk.bat` FogMap вызовом release-скрипта (опциональный флаг/шаг после сборки); проверка: полный прогон «сборка → релиз» одной последовательностью команд

## 4. Генератор витрины

- [ ] 4.1 Реализовать чтение `registry.yaml` + валидацию ключей `[a-z0-9-]` + fetch `releases.latest` и `download_count` через GitHub API с `GITHUB_TOKEN`; проверка: юнит-прогон на данных FogMap выводит JSON карточки (версия, размер, дата, ссылки)
- [ ] 4.2 Реализовать чтение `store.yaml`/скриншотов с валидацией контракта: битый проект → skip + warning в лог, ноль падений; проверка: тест на заведомо битую запись проходит, сборка завершается успешно
- [ ] 4.3 Реализовать авто-детект платформ по маскам (`*.apk`, `*-win*.zip`, `*.exe`), игнор неизвестных ассетов, группировку нескольких ассетов одной платформы в варианты; проверка: тест с мультиассетным релизом показывает оба варианта, неизвестный ассет отсутствует
- [ ] 4.4 Сгенерировать главную (сетка карточек, фильтр Android/Windows, буквенные заглушки) и страницы приложений (описание, requirements, галерея, кнопки, «Скачиваний: N», «Исходники», подсказка SmartScreen для Windows); проверка: локальный прогон generate.py даёт валидный HTML, все страницы открываются
- [ ] 4.5 Сгенерировать страницу «О проекте», навигацию, буквенный favicon, meta-теги, sitemap.xml; проверка: sitemap покрывает все страницы, favicon отдаётся локально

## 5. Оформление (dark-first)

- [ ] 5.1 Сверстать дизайн-токены: CSS-переменные, system font stack, тёмная тема по умолчанию, hover-подъём карточек, mobile-first адаптив; проверка: страница корректна на ширине 360px и 1440px
- [ ] 5.2 Реализовать переключатель темы (system/dark/light, `<10` строк JS, `data-theme` на `<html>`); проверка: выбор сохраняется между перезагрузками, по умолчанию dark
- [ ] 5.3 Встроить счётчик GoatCounter одной строкой в шаблон (site ID из задачи 1.4); проверка: страница счётит посещения в панели GoatCounter после тестового захода

## 6. CI и деплой

- [ ] 6.1 Написать workflow `.github/workflows/build.yml`: cron каждые 30 мин + триггер по push `registry.yaml`/шаблонов + `workflow_dispatch`, шаг генерации с `GITHUB_TOKEN`; проверка: ручной запуск workflow зелёный, артефакт статики собран
- [ ] 6.2 Настроить деплой статики на GitHub Pages из workflow; проверка: после push витрина доступна по `https://dziominpavel.github.io`
- [ ] 6.3 Проверить end-to-end: правка description в FogMap + push → в течение цикла описание обновилось; новый релиз → версия/размер/ссылки обновились; проверка: оба сценария наблюдаемы на опубликованной витрине

## 7. Волна на остальные 6 проектов

- [ ] 7.1 Написать скрипт-обновитель, разносящий эталон release-скрипта по репо (с проверкой версии скрипта); проверка: все 7 копий после разноса имеют одинаковую версию
- [ ] 7.2 Во всех 6 проектах завести файл `version` (текущие версии), `store.yaml`, иконки (для YandexMusicDownloader и instagram-tracker — png 512×512 от владельца), `screenshots/`; проверка: валидация контракта проходит для всех 6
- [ ] 7.3 Добавить 6 записей в `registry.yaml` стора; проверка: generate.py локально видит 7 проектов
- [ ] 7.4 YandexMusicDownloader: добавить шаг упаковки `app.exe`+`ffmpeg.exe` в `Compress-Archive` → `dist/YandexMusicDownloader-X.Y.Z-win-x64.zip`; проверка: zip распаковывается и приложение запускается
- [ ] 7.5 instagram-tracker: добавить сборку PyInstaller `--onefile` → `dist/InstagrammTracker-X.Y.Z-win-x64.exe`; проверка: exe запускается на чистой машине без Python, requirements на странице отражают специфику
- [ ] 7.6 Выпустить релизы во всех 6 проектах release-скриптом; проверка: у всех 7 репо есть актуальный релиз с ассетами
- [ ] 7.7 Финальная сквозная проверка: 7 карточек на главной, у каждой — версия/размер/дата/счётчик/кнопки/«Исходники»; фильтры и страницы работают; витрина обновилась автоматически после релизов; проверка: чек-лист пройден на опубликованном сайте
