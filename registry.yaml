# Реестр проектов витрины «Мои приложения».
# Ключ записи — слаг страницы приложения (/[key]/), паттерн [a-z0-9-].
# Добавление приложения = одна запись здесь (при выполненном контракте в его репо).
projects:
  fogmap:
    repo: dziominpavel/FogMap
